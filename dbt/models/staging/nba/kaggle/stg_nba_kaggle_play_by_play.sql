{{ config(materialized='table') }}

select
    safe_cast(`clock` as string) as clock,
    safe_cast(`actionType` as string) as action_type,
    safe_cast(`description` as string) as description,
    safe_cast(`playerFullName` as string) as player_full_name,
    safe_cast(`teamTricode` as string) as team_tricode,
    safe_cast(`gameId` as int64) as game_id,
    safe_cast(`gameDateTimeEst` as timestamp) as game_date_time_est,
    safe_cast(`periodType` as string) as period_type,
    safe_cast(`period` as int64) as period,
    safe_cast(`shortFormattedClock` as string) as short_formatted_clock,
    safe_cast(`timeActual` as timestamp) as time_actual,
    safe_cast(`actionNumber` as int64) as action_number,
    safe_cast(`orderNumber` as int64) as order_number,
    safe_cast(`actionId` as int64) as action_id,
    safe_cast(`teamId` as int64) as team_id,
    safe_cast(`side` as string) as side,
    safe_cast(`location` as string) as location,
    safe_cast(`playerteamCity` as string) as player_team_city,
    safe_cast(`playerteamName` as string) as player_team_name,
    safe_cast(`opponentteamCity` as string) as opponent_team_city,
    safe_cast(`opponentteamName` as string) as opponent_team_name,
    safe_cast(`scoreHome` as string) as score_home,
    safe_cast(`scoreAway` as string) as score_away,
    safe_cast(`possession` as int64) as possession,
    safe_cast(`isTargetScoreLastPeriod` as int64) as is_target_score_last_period,
    safe_cast(`subType` as string) as sub_type,
    safe_cast(`descriptor` as string) as descriptor,
    safe_cast(`qualifiers` as struct<list array<struct<element string>>>).list as qualifiers,
    safe_cast(`edited` as string) as edited,
    safe_cast(`value` as string) as value,
    safe_cast(`personId` as int64) as person_id,
    safe_cast(`playerName` as string) as player_name,
    safe_cast(`playerNameI` as string) as player_name_initial,
    safe_cast(`jerseyNumber` as string) as jersey_number,
    safe_cast(`x` as float64) as x,
    safe_cast(`y` as float64) as y,
    safe_cast(`xLegacy` as int64) as x_legacy,
    safe_cast(`yLegacy` as int64) as y_legacy,
    safe_cast(`area` as string) as area,
    safe_cast(`areaDetail` as string) as area_detail,
    safe_cast(`isFieldGoal` as int64) as is_field_goal,
    safe_cast(`shotDistance` as float64) as shot_distance,
    safe_cast(`shotValue` as int64) as shot_value,
    safe_cast(`shotResult` as string) as shot_result,
    safe_cast(`pointsTotal` as int64) as points_total,
    safe_cast(`shotActionNumber` as int64) as shot_action_number,
    safe_cast(`assistPersonId` as int64) as assist_person_id,
    safe_cast(`assistFullName` as string) as assist_full_name,
    safe_cast(`assistPlayerName` as string) as assist_player_name,
    safe_cast(`assistPlayerNameInitial` as string) as assist_player_name_initial,
    safe_cast(`assistTotal` as int64) as assist_total,
    safe_cast(`reboundTotal` as int64) as rebound_total,
    safe_cast(`reboundDefensiveTotal` as int64) as rebound_defensive_total,
    safe_cast(`reboundOffensiveTotal` as int64) as rebound_offensive_total,
    safe_cast(`turnoverTotal` as int64) as turnover_total,
    safe_cast(`stealPersonId` as int64) as steal_person_id,
    safe_cast(`stealFullName` as string) as steal_full_name,
    safe_cast(`stealPlayerName` as string) as steal_player_name,
    safe_cast(`stealTotal` as int64) as steal_total,
    safe_cast(`foulPersonalTotal` as int64) as foul_personal_total,
    safe_cast(`foulTechnicalTotal` as int64) as foul_technical_total,
    safe_cast(`foulDrawnPersonId` as int64) as foul_drawn_person_id,
    safe_cast(`foulDrawnFullName` as string) as foul_drawn_full_name,
    safe_cast(`foulDrawnPlayerName` as string) as foul_drawn_player_name,
    safe_cast(`blockPersonId` as int64) as block_person_id,
    safe_cast(`blockFullName` as string) as block_full_name,
    safe_cast(`blockPlayerName` as string) as block_player_name,
    safe_cast(`blockTotal` as int64) as block_total,
    safe_cast(`jumpBallRecoveredName` as string) as jump_ball_recovered_name,
    safe_cast(`jumpBallRecoveredPersonId` as int64) as jump_ball_recovered_person_id,
    safe_cast(`jumpBallRecoveredFullName` as string) as jump_ball_recovered_full_name,
    safe_cast(`jumpBallWonPersonId` as int64) as jump_ball_won_person_id,
    safe_cast(`jumpBallWonFullName` as string) as jump_ball_won_full_name,
    safe_cast(`jumpBallWonPlayerName` as string) as jump_ball_won_player_name,
    safe_cast(`jumpBallLostPersonId` as int64) as jump_ball_lost_person_id,
    safe_cast(`jumpBallLostFullName` as string) as jump_ball_lost_full_name,
    safe_cast(`jumpBallLostPlayerName` as string) as jump_ball_lost_player_name,
    safe_cast(`subsInPersonId` as int64) as subs_in_person_id,
    safe_cast(`subsInFullName` as string) as subs_in_full_name,
    safe_cast(`subsInPlayerName` as string) as subs_in_player_name,
    safe_cast(`officialId` as int64) as official_id,
    safe_cast(`videoAvailable` as int64) as video_available,
    safe_cast(`personIdsFilter` as struct<list array<struct<element string>>>).list as person_ids_filter,
    safe_cast(`gameType` as string) as game_type,
    safe_cast(`playerteamId` as int64) as player_team_id,
    safe_cast(`opponentteamId` as int64) as opponent_team_id,
    safe_cast(`jumpBallRecoverdPersonId` as int64) as jump_ball_recoverd_person_id,
    safe_cast(`gameDateTimeEst_g` as timestamp) as game_date_time_est_g,
    safe_cast(`gameType_g` as string) as game_type_g

from {{ source('nba_raw', 'play_by_play') }}
